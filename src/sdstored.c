// Server
#include "helper.h"
#define SIZE 128

typedef struct Queue {
    int front, rear, size;
    unsigned capacity;
    // INSERT HERE THE DATA STORED
} * queue;

typedef struct Transformation{
    char op_name[SIZE];
    char file_name[SIZE];
} transformaion;

int main(int argc, char *argv[]){

    return 0;
}